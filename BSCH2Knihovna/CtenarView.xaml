<Window x:Class="BSCH2Knihovna.CtenarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reader and Loan Management" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="0,0,0,5">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <TextBox Width="200" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding FilteredCtenari}" SelectedItem="{Binding SelectedCtenar}" 
                  AutoGenerateColumns="False" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Visibility="Collapsed"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Jmeno}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Prijmeni}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Telefon}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,10" VerticalAlignment="Top">
            <TextBlock Text="First Name:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding EditingCtenar.Jmeno, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Last Name:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding EditingCtenar.Prijmeni, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Phone:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding EditingCtenar.Telefon, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Email:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding EditingCtenar.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Add" Command="{Binding AddCtenarCommand}" Width="75" Margin="5,0"/>
                <Button Content="Update" Command="{Binding UpdateCtenarCommand}" Width="75" Margin="5,0"/>
                <Button Content="Delete" Command="{Binding DeleteCtenarCommand}" Width="75" Margin="5,0"/>
            </StackPanel>
        </StackPanel>

        <DataGrid ItemsSource="{Binding BorrowedBooks}" AutoGenerateColumns="False"
                  Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Book" Binding="{Binding BookName}" Width="*"/>
                <DataGridTextColumn Header="Loan Date" Binding="{Binding DatumVypujceni}" Width="*"/>
                <DataGridTextColumn Header="Return Due Date" Binding="{Binding DatumVratenka}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
