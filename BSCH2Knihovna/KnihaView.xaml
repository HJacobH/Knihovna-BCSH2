<Window x:Class="BSCH2Knihovna.KnihaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BSCH2Knihovna"
        mc:Ignorable="d"
        Title="Správa Knih" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Knihy}" SelectedItem="{Binding SelectedKniha}" AutoGenerateColumns="False" Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="*"/>
                <DataGridTextColumn Header="Název" Binding="{Binding Nazev}" Width="*"/>
                <DataGridTextColumn Header="Autor" Binding="{Binding Autor}" Width="*"/>
                <DataGridTextColumn Header="Rok Vydání" Binding="{Binding RokVydani}" Width="*"/>
                <DataGridTextColumn Header="Nakladatelství" Binding="{Binding Nakladatelstvi}" Width="*"/>
                <DataGridTextColumn Header="Sekce Kategorie" Binding="{Binding Zanr}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Top">
            <TextBlock Text="ISBN:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding SelectedKniha.ISBN, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Název:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding SelectedKniha.Nazev, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Autor:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding SelectedKniha.Autor, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Rok Vydání:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding SelectedKniha.RokVydani, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Nakladatelství:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding SelectedKniha.Nakladatelstvi, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Text="Sekce Kategorie:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox Width="200" ItemsSource="{Binding SekceList}" DisplayMemberPath="Kategorie" 
                      SelectedItem="{Binding SelectedSekce, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add" Command="{Binding AddKnihaCommand}" Width="75" Margin="5,0"/>
            <Button Content="Update" Command="{Binding UpdateKnihaCommand}" Width="75" Margin="5,0"/>
            <Button Content="Delete" Command="{Binding DeleteKnihaCommand}" Width="75" Margin="5,0"/>
        </StackPanel>
    </Grid>
</Window>
